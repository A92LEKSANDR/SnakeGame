#include "constants.h"

namespace consts {
	const int tileSize = 25;
	const int countTileWeight = 20;
	const int countTileHeight = 30;
	int weightWindow = tileSize * countTileWeight;
	int heightWindow = tileSize * countTileHeight;
}
